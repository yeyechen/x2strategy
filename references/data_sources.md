# Data Sources

All data lives in ClickHouse.  Connection details are in ``.env``;
the auto-discovered schema catalog is at
``paper2spec/resources/clickhouse_catalog.json``.

For query patterns, safety rules, and schema discovery, see
[references/clickhouse.md](clickhouse.md).
